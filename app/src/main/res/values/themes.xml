<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GliaAndroidSDKWidgets" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_secondary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <item name="materialButtonStyle">@style/Application.GliaAndroidSdkWidgets.Button</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Application.GliaAndroidSdkWidgets.Button" parent="@style/Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="android:textColor">@color/color_black_text</item>
        <item name="textAllCaps">false</item>
        <item name="iconTint">?attr/colorPrimary</item>
    </style>
</resources>
