<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="*/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <category android:name="android.intent.category.DEFAULT" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <category android:name="android.intent.category.DEFAULT" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission
        android:name="android.permission.CAMERA"
        android:required="false" />
    <!-- Required by CallForegroundService's microphone foregroundServiceType; also declared by the Core SDK manifest. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <activity
            android:name=".chat.ChatActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Glia.Internal.Chat"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".filepreview.ui.ImagePreviewActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".chat.ChatActivity"
            android:theme="@style/Theme.Glia.Internal.ImagePreview" />
        <activity
            android:name=".call.CallActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Glia.Internal" />
        <activity
            android:name=".survey.SurveyActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Glia.Internal.Translucent"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name="com.glia.widgets.messagecenter.MessageCenterActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Glia.Internal.Chat"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="com.glia.widgets.webbrowser.WebBrowserActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Glia.Internal.Chat" />

        <activity
            android:name=".entrywidget.EntryWidgetActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Glia.Internal.Translucent" />

        <activity
            android:name=".helper.DialogHolderActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Glia.Internal.Translucent" />

        <activity
            android:name=".push.notifications.PushClickHandlerActivity"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Glia.Internal.Translucent" />

        <service
            android:name=".core.chathead.ChatHeadService"
            android:enabled="true"
            android:stopWithTask="true" />

        <service android:name=".core.notification.NotificationRemovalService" />

        <service
            android:name=".internal.notification.device.CallForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone|camera"
            android:stopWithTask="false" />

        <provider
            android:name=".core.fileupload.GliaFileProvider"
            android:authorities="${applicationId}.com.glia.widgets.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider" />
        </provider>

        <provider
            android:name="com.glia.androidsdk.InitializationProvider"
            android:authorities="${applicationId}.InitializationProvider"
            tools:node="remove" /> <!-- Overrides/extends this provider in Widgets, so unregistering it -->

        <provider
            android:name=".InitializationProvider"
            android:authorities="${applicationId}.InitializationProvider"
            android:exported="false" />
    </application>
</manifest>
